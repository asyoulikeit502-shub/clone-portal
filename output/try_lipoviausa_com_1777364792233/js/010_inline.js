function activateTab(tabId,direction="left"){$(".tab-btn").removeClass("active");$(`.tab-btn[data-tab='${tabId}']`).addClass("active");$(".tab-panel").removeClass("active slide-left slide-right");let panel=$('.tab-panel[id="'+tabId+'"]');panel.addClass("active");if(direction==="left"){panel.addClass("slide-left");$(".swp_lft p").text("SWIPE RIGHT TO SEE LESS")}else{panel.addClass("slide-right");$(".swp_lft p").text("SWIPE LEFT TO SEE MORE")}}$(".tab-btn").click(function(){let tabId=$(this).data("tab");let direction=tabId==="sub"?"left":"right";activateTab(tabId,direction)});let startX=0;let startY=0;let startTime=0;const SWIPE_DISTANCE=180;const MAX_VERTICAL=80;const MAX_TIME=500;$(".for_mob_pack").on("touchstart",function(e){startX=e.touches[0].clientX;startY=e.touches[0].clientY;startTime=Date.now()});$(".for_mob_pack").on("touchend",function(e){let endX=e.changedTouches[0].clientX;let endY=e.changedTouches[0].clientY;let endTime=Date.now();let diffX=startX-endX;let diffY=Math.abs(startY-endY);let timeDiff=endTime-startTime;if(diffY>MAX_VERTICAL)return;if(timeDiff>MAX_TIME)return;if(diffX>SWIPE_DISTANCE){activateTab("sub","left")}if(diffX<-SWIPE_DISTANCE){activateTab("one","right")}});